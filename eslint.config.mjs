import antfu from '@antfu/eslint-config'

export default antfu({
  // Vue support (auto-detected from package.json)
  // vue: true,

  // TypeScript support (auto-detected)
  // typescript: true,

  // Formatter rules
  formatters: true,
})
